<h1>Listing tasks</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Done</th>
    <th>List</th>
  </tr>

<% @tasks.each do |task| %>
  <tr>
    <td><%= task.title %></td>
    <td><%= task.done %></td>
    <td><%= task.list %></td>
  </tr>
<% end %>
</table>

<br />
