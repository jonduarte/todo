<div class="page-header">
  <h1>Public Lists</h1>
</div>

<% @lists.each do |list| %>
  <div class="well" id="<%= dom_id list %>">
    <%= render list %>
    <%= button_to "favorite", favorite_path(list.id), :method => :put, :remote => true, :class => "btn btn-mini btn-info" %>
  </div>
<% end %>
